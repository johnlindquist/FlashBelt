####################################### VERSION 1.0 ###################################
Generator.author            = John Lindquist

################################ TEMPLATE PROPERTIES ##################################
templates.dir			    = ${build.dir}/templates/
robotlegs.templates.dir     = ${build.dir}/templates/robotlegs
flight.templates.dir        = ${build.dir}/templates/flight
signals.templates.dir       = ${build.dir}/templates/signals

################################ DIRECTORIES ################################
control.dir                 = controller
model.dir                   = model
view.dir                    = view
vo.dir                      = vo
services.dir                = service
events.dir				    = events
signals.dir                 = signals

################################ PROTECTED PROPERTIES #################################
namespace                   = ${com.dir}.${domain.dir}.${project.dir}
project.main.url            = ${workspace.dir}/${project.dir}/${src.dir}
project.root.url            = ${workspace.dir}/${project.dir}/
project.url                 = ${workspace.dir}/${project.dir}/${src.dir}/${project.path}